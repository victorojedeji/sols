const baseUrl = "/api";

//auth url
export const SIGNUP_URL = `${baseUrl}/auth/sign-up`;
export const LOGIN_URL = `${baseUrl}/auth/log-in`;

//products url

export const PRODUCTS_URL = `${baseUrl}/products`;

//category url
export const CATEGORIES_URL = `${baseUrl}/categories`;

//cart url
export const CART_URL = `${baseUrl}/user/cart`;
